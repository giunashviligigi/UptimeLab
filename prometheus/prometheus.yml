global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: uptimelab-api
    metrics_path: /metrics
    static_configs:
      # Service name on Docker Compose network (use with: make obs-up)
      - targets: ["backend:8080"]
