# Example Prometheus config — run Prometheus separately or add to compose later
global:
  scrape_interval: 15s

scrape_configs:
  - job_name: uptimelab-api
    metrics_path: /metrics
    static_configs:
      - targets: ["backend:8080"]
